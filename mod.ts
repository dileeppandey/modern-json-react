/**
 * A production-grade JSON editor React component with tree view,
 * code editing, JSON Schema validation, and accessibility-first design.
 *
 * @example
 * ```tsx
 * import { JsonEditor } from "@dileep/modern-json-react";
 *
 * function App() {
 *   return (
 *     <JsonEditor
 *       initialValue='{"hello": "world"}'
 *       onChange={(value) => console.log(value)}
 *     />
 *   );
 * }
 * ```
 *
 * @module
 */
export * from './src/index.ts';
